<%- include('../partials/header') %>
<table id="list">
  <thead>
    <tr>
      <th>Title</th>
      <th>Author</tr>
      <th>Genre</th>
      <th></th>
    </tr>
  </thead>
  <tbody>
    <% movies.forEach(function(b) { %>
      <tr>
        <td><%= b.title %></td>
        <td><%= b.releaseYear %></td>
        <td><a href="/books/<%= b._id %>">DETAILS</a></td>
        <td><a href="/books/<%= b._id %>/favorite">ADD TO FAVORITES</a></td>
      </tr>
      <% }); %>
    </tbody>
  </table>
  
<%- include('../partials/footer') %>